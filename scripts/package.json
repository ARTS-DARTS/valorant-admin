{
  "name": "vlineups-cron",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "cleanup": "node cleanup_bots.js",
    "test": "node --test cleanup_auth.test.js"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  }
}
